--- settings.py.orig	2012-12-03 22:17:55.000000000 +0100
+++ settings.py	2012-12-07 20:22:55.651241810 +0100
@@ -11,10 +11,10 @@
 
 DATABASES = {
     'default': {
-        'ENGINE': 'django.db.backends.sqlite3', # Add 'postgresql_psycopg2', 'mysql', 'sqlite3' or 'oracle'.
-        'NAME': 'dbase.db',                      # Or path to database file if using sqlite3.
-        'USER': '',                      # Not used with sqlite3.
-        'PASSWORD': '',                  # Not used with sqlite3.
+        'ENGINE': 'django.db.backends.mysql', # Add 'postgresql_psycopg2', 'mysql', 'sqlite3' or 'oracle'.
+        'NAME': 'djangopflege', # Or path to database file if using sqlite3.
+        'USER': 'django',                      # Not used with sqlite3.
+        'PASSWORD': 'jsrdjango',                  # Not used with sqlite3.
         'HOST': '',                      # Set to empty string for localhost. Not used with sqlite3.
         'PORT': '',                      # Set to empty string for default. Not used with sqlite3.
     }
@@ -67,7 +67,7 @@
     # Put strings here, like "/home/html/static" or "C:/www/django/static".
     # Always use forward slashes, even on Windows.
     # Don't forget to use absolute paths, not relative paths.
-    'C:/Users/jan/Documents/GitHub/djangopflege/static',
+    '/srv/http/djangopflege/static',
 )
 
 # List of finder classes that know how to find static files in
@@ -107,7 +107,7 @@
     # Put strings here, like "/home/html/django_templates" or "C:/www/django/templates".
     # Always use forward slashes, even on Windows.
     # Don't forget to use absolute paths, not relative paths.
-    'C:/Users/jan/Documents/GitHub/djangopflege/templates'
+    '/srv/http/djangopflege/templates'
 )
 
 INSTALLED_APPS = (
@@ -153,4 +153,4 @@
 }
 
 LOGIN_URL = '/login/'
-LOGOUT_URL = '/logout/'
\ No newline at end of file
+LOGOUT_URL = '/logout/'
